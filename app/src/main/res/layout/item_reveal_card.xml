<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:orientation="vertical"
    android:padding="14dp">

    <!-- Header row: label + points -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/tv_result_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="CORRECT CATCH"
            android:textColor="@color/colorCorrect"
            android:textSize="11sp"
            android:textStyle="bold"
            android:letterSpacing="0.12" />

        <TextView
            android:id="@+id/tv_points"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+20"
            android:textColor="@color/colorCorrect"
            android:textSize="20sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- What was flagged or missed -->
    <TextView
        android:id="@+id/tv_reveal_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="The flagged or missed text"
        android:textColor="@color/colorTextPrimary"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.4" />

    <!-- Explanation -->
    <TextView
        android:id="@+id/tv_reveal_explanation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Explanation"
        android:textColor="@color/colorTextSecondary"
        android:textSize="13sp"
        android:lineSpacingMultiplier="1.45" />

</LinearLayout>